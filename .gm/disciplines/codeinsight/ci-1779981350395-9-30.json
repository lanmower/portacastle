./src/app/api/files/read/route.ts:9:18 GET
async function GET(req: Request) {
  const url = new URL(req.url);
  if (!url.searchParams.get("path")) {
    return NextResponse.json(
      { error: "workspaceId and path required" },
      { status: 400 },
    );
  }
  return proxy(req);
}