X-SERVER fork+exec UNDER BLINK -- exhaustive state (2026-05-29, ~32 CI cycles). The real Alpine Xvfb runs under blink THROUGH: LLVM-OOM (stub libGL), epoll (poll shim), display-lock (-nolock), socket transport (finite RLIMIT_NOFILE + in-process AF_UNIX layer), device init -- ALL SOLID + unregressed. The LAST gate is the XKB keymap which needs fork+exec (Xorg Popen). FULL CHAIN MAPPED: Xvfb fork#0=startup daemonize (no exec; returning 0 -> Xvfb exit 127; ENOSYS -> Xvfb tolerates+reaches keymap, PROVEN); fork#1=Popen execs /bin/sh -c xkbcomp; sh (busybox, runs standalone) fork#2 execs xkbcomp (runs standalone EXIT 0). IMPLEMENTED in lanmower/blink@libblink-portabox syscall.c (__EMSCRIPTEN__): in-VM synchronous vfork-exec with a NESTING STACK (g_em_fork_stack/depth), first-top-level-fork=ENOSYS (g_em_fork_total reset per top-level run in blinkenlib SetUp), EmRunChildInline(NewSystem+NewMachine sharing parent fds via AddStdFd + trapexit + sigsetjmp+Actor), SysExecve pops+runs-inline+siglongjmp, SysWait4 reaps synthetic pids. fork/vfork/wait4 registered under __EMSCRIPTEN__ without HAVE_FORK (HAVE_FORK regresses Xvfb). CURRENT BLOCKER: Xvfb still exits 127 with EMPTY stderr when the keymap fork enters the in-VM model -- EmRunChildInline running /bin/sh inline appears to abort/corrupt the process (reentrant Actor + child LoadProgram, possibly InitMap/InitBus re-entry, unassert, or child exit() killing the process instead of trapexit-longjmp). blink fprintf(stderr) is NOT visible (emscripten routes to r.stderr which is empty; getenv reads Module ENV not process.env so env-gated traces are dead; MEMFS markers during fork corrupt state but may be safe from EmRunChildInline). NEXT: marker from inside EmRunChildInline to see how far it gets; verify child trapexit longjmp target + that child exit doesnt call host _exit; check NewSystem/InitMap re-entry. Webix build-blink.yml has the X-smoke + xkbcomp/sh standalone probes (both run fine standalone). Markers /tmp/blink-*-fired corrupt Xvfb if written during fork -- avoid.