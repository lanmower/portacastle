sandcastle/portabox blink rebuild progress: Fork lanmower/blink cloned to C:/dev/blinkfork, working branch libblink-portabox created (git user lanmower). Confirmed: JIT impossible under wasm32 (jit.c:56 x86-64/aarch64 only) so max-perf=threads+sockets+pipes+fork NOJIT. pipe/threads/sockets all present in C, gated only by configure --disable-all; enable via --enable-threads/--enable-sockets/--enable-fork/--enable-nonposix. argv bug: blinkenlib.c:343 stringToArgsArray splits on space (strtok ' '), breaks multi-word args; fix=NUL-separated marshalling. Framebuffer: guest publishes fb vaddr to host, host reads via blinkenlib_spy_address (blinkenlib.c:479). Need guest->host fb-register channel = add a synthetic syscall (table at syscall.c:5630+). Local Docker DOWN, build via GitHub Actions build-blink.yml (webix repo, emsdk 3.1.69). Smoke test expects exit 42 from hello-x86_64.elf.