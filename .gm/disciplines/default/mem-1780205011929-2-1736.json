SANDCASTLE RESIDENT-VM EPIC — PRECISE PLAN (2026-05-31, after perf pass 3). The ONLY remaining fps lever (per-frame VM respawn = 331ms warm, witnessed; everything else <2ms). Render-once is deliberate (xappdemo.c xappdemo_main: mmap+drain-input+paint+fb_register(0x5fb)+SYS_exit per frame; webix/containers/xappdemo.c:179). Closure = a coordinated 3-repo + CI epic, decomposed as a Maximal Cover DAG: (1) blinkenlib-blocking-input-syscall [blinkenlib.c wasm]: add a BLOCKING/timeout input-read (current 0x5fc is non-blocking, so a resident while(1) loop would busy-spin the worker at 100%); this is the wasm-level prerequisite. (2) resident-vm-persistent-runloop [webix blink-core.js + guest xappdemo.c]: rewrite xappdemo to mmap+fb_register ONCE then while(1){block-on-input; update; paint; publish}; add a startForegroundVM/run-loop slot in blink-core (like startXServer's persistent slot-0 model at blink-core.js:416, but for slot 2 foreground) that run_thread_slot's the resident ELF once + an always-on proxy pump. (3) sab-fb-and-input-ring [webix+portabox]: main thread blits the SAB fb on rAF + writes input into the SAB ring directly, no per-frame displayPixels copy / pushInput proxy. WITNESS for all three = a CI wasm/ELF rebuild (webix build-blink.yml compiles containers/*.c + threaded blinkenlib.wasm) + redeploy to sandcastle public/containers + browser re-witness fps>>3. This exceeds an in-session in-browser witness (CI is minutes, async), so it is a dedicated build-session epic, NOT app-JS-reachable like pass-3's fixes. The JS-level re-entrancy fix (webix 1977562) already put frames 2+ on a worker thread; resident-VM removes the respawn. Reachable in my repos (AnEntrypoint/webix + blink), not authority-blocked.