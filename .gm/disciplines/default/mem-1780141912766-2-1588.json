X APPS COMPLETE IN-BROWSER END-TO-END (2026-05-30). The sandcastle X Apps app runs a real Xvfb + real xdpyinfo concurrently under blink IN A REAL BROWSER TAB (no server) and renders the full xdpyinfo display dump (display :99, X.Org 21.1.16, 640x480x16, 21 extensions), client exit 0 -- WITNESSED live via playwriter (localhost:3000 /desktop/workspace, __sc.window.openWindow x-apps). TWO fixes closed the in-browser gap (CI node already worked): (1) blink-core.js runConcurrent poll loop now calls Module._emscripten_main_thread_process_queued_calls() each tick (exported via build FUNCS) -- in-browser the X server+client run on emscripten pthreads (Web Workers) whose blocking syscalls proxy to the main thread; the bare await-sleep poll never serviced the proxy queue so thread_done_slot(1) never flipped (Running… forever); node libuv scheduled the threads so CI was unaffected. (2) sandcastle XAppsApp writes the overlay DIRECTLY via the live Module.FS (sb._client.host.core.Module.FS open/write/close/chmod) instead of sb.writeFiles -- the high-level Sandbox.writeFiles did NOT land files in the in-browser guest MEMFS (awaited+fast but files MISSING -> runConcurrent fs.readFile ENOENT). OPEN portabox bug: Sandbox.writeFiles in-browser not landing (portabox-writefiles-inbrowser-not-landing) -- worked around in the product. Also: removed the serial runCommand(mkdir) that wedged the runExclusive chain (fold /tmp/.X11-unix into the batch). Commits: webix 11b58cb (pump+export), sandcastle 2b0671f (direct-FS-write + pump wasm). diagnostic phase-marks live on window.__xappdbg.