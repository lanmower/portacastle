APK RUN-WHATEVER -- FULL DOCK FLOW WITNESSED E2E (2026-05-31, session11 browser :3000). Confirms the apk capability shipped in 0c0c109 works from the actual product path a user takes. DOCK FLOW (browser-509/510/511, fresh ws-xbo40tas): double-click xterm dock entry -> x11 window mounts -> overlay 'Installing xterm (apk)...' (xinstallByWorkspace populated) -> install completes (~tens of seconds, xterm + ~30-pkg dep closure over CORS proxy) -> overlay clears (xinstall=null) -> runConcurrent runs xterm against Xvfb -> XWindowCanvas lastGen=23 (fb generation advancing = X server live + xterm CONNECTED over AF_UNIX), lastErr=null, paints0/blankFrames1 (all-zero fb) -> never-silent-black diagnostic overlay 'X server is running but produced no display output yet...'. VERDICT: install+exec+connect all WORK in-page for an arbitrary non-bundled X client; the VISIBLE pixel paint is gated by the external all-zero-Xvfb-fb frontier (blink, mem-1780183969360) -- identical for bundled (xeyes/xclock) and apk-installed (xterm) clients, so this is not an apk limitation. Installs also witnessed: figlet (66 files), xterm (dep closure), xset v1.2.5-r1 -- all from real alpine v3.21 repos via codetabs CORS proxy. LIMITATIONS (existing, documented, not regressions): (1) one Xvfb per workspace owns display :99 -- a 2nd concurrent X client via direct runConcurrent collides ('server already running / Cannot establish any listening sockets'); concurrent launches must serialize through runExclusive (the dock path does). (2) ensureXStack (lays Xvfb+keymap) runs only via launchXApp/X-Apps, not a GUI-only boot; window.__sc.launchXApp self-attaches only once an x11 app has launched via the dock. RUN X APPS ONE-AT-A-TIME PER WORKSPACE. The webix CI XC-smoke proves apk-client exit 0 on the same artifacts in node.