X-APPS DISPLAY RE-VALIDATION SWEEP #2 (2026-05-31, session13 browser :3000 fresh /desktop/workspace, guest). Re-validated EVERY dock X app live + FIXED a real silent-black bug. PER-APP VERDICT: xeyes (browser-335/336: ticks503 paints0 lastGen5 -> all-zero Xvfb fb external + diagnostic overlay shown CORRECTLY, never silent black). xclock (browser-339: 2nd canvas, diagnostic overlay). xcalc (browser-343: paints1 blankFrames0 -> PAINTED content, overlay correctly cleared). X Apps xdpyinfo (browser-356/357 ISOLATED workspace: runConcurrent client exit 0 +6377ms, renders full dump X.Org 21.1.16 :99). GUI xappdemo (browser-360/362/366: ELF execs clean lastErr=null -- PRIOR EXTERNAL VM memory-access FAULT NOT REPRODUCING this session; clean workspace frames1 blits1 canvas 4782 nonzero px gradient[20,30,60] = RENDERS CONTENT). FIX SHIPPED: GuiDesktopApp.tsx had a SILENT-BLACK bug -- overlay only showed when status!=running, so a running-but-never-blitted GUI was a mute black canvas (XWindowCanvas got the never-silent-black treatment in 3aa1892 but GUI did not). Added hasPainted+noOutput state; overlay now shows when (status!=running || !hasPainted), with 'GUI is running but produced no framebuffer output yet' diagnostic when frames>=3 && blits==0. tsc+eslint+build:static all exit 0, browser-witnessed (blits1->overlay hidden, content path intact). xterm still absent from dock (browser-332: Terminal,Files,Code,GUI,X Apps,xeyes,xclock,xcalc only). Window close cleanup witnessed (browser-372: canvas 1->0 on close). TWO documented findings (not display bugs, recorded for worker-scheduling track): (1) X Apps one-shot runConcurrent queues behind persistent X11 clients on the single per-workspace VM lock (~84s) -- resolves in isolated workspace; (2) __sc.xwindowByWorkspace debug mirror is per-workspace last-window-wins (display per-window is fine). All-zero Xvfb fb root cause stays EXTERNAL (lanmower/blink). NOTE build:static EPERM rename src/app/api needs ALL next node procs killed first (dev+postcss+server), and revert any leftover _api_excluded/_admin_excluded before retry.