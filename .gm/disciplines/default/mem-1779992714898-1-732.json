BLINK THREADED BUILD: -pthread COMPILES + LINKS GREEN (CI build 26593765719 sha 0a2ebdf, step 7 Compile success, step 8 staged). The config.h force-defines approach works. KEY RUNTIME FIX: under -pthread emscripten uses --import-memory --shared-memory, so the WASM memory is IMPORTED not exported -> Module.wasmExports.memory is UNDEFINED. blink-core.js must read the live ArrayBuffer from Module.HEAPU8.buffer (preferred, always re-pointed on growth) or Module.wasmMemory.buffer, NOT wasmExports.memory.buffer. Added memBuffer(Module) helper routing all 6 memory accesses (snapshot/restore/readRegisters/writeStr/writeArgv/fbView) through it. emscripten 3.1.69. This was the only blink-core change needed for the threaded artifact.