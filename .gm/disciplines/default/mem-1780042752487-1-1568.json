SANDCASTLE PERF PASS (2026-05-29). Eliminated per-frame GUI bottlenecks + first-load, all browser-witnessed, 3 repos pushed (sandcastle bdeec11, webix 1514127, portabox 115373c). WHAT LANDED: (1) GuiDesktopApp.tsx blit path: reuse ONE ctx.createImageData backbuffer (was alloc 1.9MB/frame); schedule via requestAnimationFrame not setTimeout; gate blit on view.generation!==lastGen AND a guest damage token; fetch ELF with cache:no-store so rebuilds land. (2) xappdemo.c emits trailing stdout damage token "... ! d" (1=window moved/drag-flip/fresh-pointer, 0=identical); host skips the 1.9MB copy+upload on damage=0. WITNESS: lastOut=220 150 0 0 0 | 40 90 200 ! 1, frames:4 blits:3 (blits<frames proves the skip). (3) portabox host.ts: rootfs served from Cache API (webix-assets-v1); wasm already cached upstream (webix-wasm-v1). (4) webix build-blink.yml now rebuilds guest ELFs on containers/*.c (was ONLY triggering on its own path -> guest-app edits silently never shipped). KEY FACTS: portabox is a file:../sandbox symlink dep -> MUST npm run build in C:/dev/sandbox after editing its src for sandcastle to pick it up. codesearch index (.gm/disciplines/codeinsight) was pre-rewire-stale; pruned 23 path-dead entries; 20 surviving entries are CONTENT-stale (need plugkit indexer reindex, no spool verb). REMAINING PERF: worker-scheduling (VM-in-worker, the architectural ceiling -> render-once exec per frame is the dominant cost, threads enabled in CI build per the hard gate); IDBFS persistence; first-boot progress UI; filemanager busybox find -unrecognized bug.