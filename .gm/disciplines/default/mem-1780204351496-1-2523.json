SANDCASTLE PERF PASS 3 — LANDED + PUSHED (2026-05-31, profiling session). Built live perf harness (src/lib/perf.ts, window.__sc.perf) and found+fixed the conclusive bottleneck. COMMITS: sandcastle [6cd7984 harness+gen-gate+parse-fix, 0bf496d leak+coalesce+xpra-branch, e2d76b9 full-xpra-excision], webix [1977562 runElf re-entrancy]. ALL browser-witnessed live (localhost:3007).

HEADLINE: per-frame VM exec dominates ~400x (warm 331ms vs displayInfo 0.04 / displayPixels 0.83 / blit 0.48 / stdout 0.10 ms). fps ceiling = the render-once VM RESPAWN per frame, addressable only by a resident VM. CRITICAL BUG FOUND+FIXED: webix blink-core runElf used the main-thread _blinkenlib_run() which is single-shot in-browser (2nd call's InitBus re-init aborts) -> the GUI desktop NEVER animated past frame 1 + every sequential runCommand wedged. FIX (webix 1977562): after the first run, route runElf through the proven re-entrant worker-slot path (vm_spawn -> run_thread_slot(slot 2) -> poll thread_done_slot + pump proxy queue -> thread_status_slot); witnessed GUI frames 1->8, terminal sequential cmds 95-152ms each (no wedge).

LANDED: (1) perf harness. (2) runElf re-entrancy [the big one]. (3) gen-gate: probe displayInfo().generation before the 1.9MB displayPixels copy (idle frames ~0). (4) GUI stdout parse hardened (strip '$ xappdemo' prompt prefix, damage default 1 on empty token). (5) persist-flush setInterval leak fixed (Map<wsId,teardown>, cleared in disposeSandbox; witnessed threw=null). (6) X pointer-motion coalesced to 1/rAF. (7) full xpra excision: deleted xpra-store + 4 components + type + the xpra-html5-client dep, rewired 5 consumers (NotificationToasts/SystemTray/resolve-window-icon/FileManager->launchXApp/MobileWindowStack->XWindowCanvas/desktop-shell).

MEASURED-NEGLIGIBLE (resolved, NOT optimized — all <2ms vs 331ms exec): blit putImageData, RGB565->RGBA, stdout round-trip, GC churn, damage-rect. VERIFIED-FINE: cold boot 630ms warm-cache (wasm+rootfs Cache-API+lazy+instantiateStreaming already), idle=0 frames, heap bounded 32->33MB, runChain no growth, attachDisplay already gen-gates (display.js:48), wasm served application/wasm on Pages+local.

REMAINING EPIC (decomposed, needs guest-ELF loop rewrite + wasm rebuild via CI): resident-vm-persistent-runloop (exec ONCE, loop in worker -> 331ms->~0) + sab-fb-and-input-ring. This is the SOLE remaining fps lever. OPEN: prod-bundle-analyze (blinkenlib.js 80KB glue loads early in dev though wasm is lazy), xpra-full-surface lockfile prune.