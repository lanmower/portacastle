X-APPS DISPLAY — SESSION VERDICT (2026-05-31, 3 fixes shipped + final blocker isolated). THE BIG WIN: the all-zero-Xvfb-framebuffer frontier (open across ALL prior sessions, long mislabeled "external") is CLOSED + pixel-proven. (1) CROSS-VM FB READ [shipped, proven]: fb host ptr resolved against caller-thread _Thread_local m; X server runs on a worker-VM pthread so the main-thread blit resolved the wrong VM -> all-zero. FIX blink 31b3b88: non-thread-local fb_machine captured at syscall 0x5fb; get_fb_ptr + new blinkenlib_fb_spy_address resolve against it. webix 33c4918 fbView uses it. PROVEN browser-655: xsetroot -solid red -> ENTIRE fb [255,0,0] fbNonzero=80000. (2) SOCKET-COLLISION [shipped]: sandcastle x-launch.ts unlinks stale /tmp/.X11-unix/X99+lock before runConcurrent (browser-629/630/631 no "server already running"). (3) SERVER POLL-FREEZE [shipped]: blink 75bafbb bumps fb_generation each poll tick from fb_machine while the X server idles in poll() (BlockHandler only runs between dispatch iterations, froze when idle). PROVEN browser-688: gen climbs ~50/s vs frozen at 11. Built blinkenlib.wasm 4da240131ce9 (build-blink.yml run 26711309243). FINAL REMAINING BLOCKER (isolated, NOT external, NOT yet fixed) = client-window-paint-scheduling: one-shot ROOT painters (xsetroot) fully work, but client WINDOWS (xclock/xeyes/xcalc/xterm) stay black even with gen climbing -> the client worker (slot1) does not get its Expose event processed + draw into the screen pixmap. Aggressive main-thread proxy pumping FAULTS (browser-692 memory access out of bounds) = the issue is the blink dual-worker concurrency/event-delivery layer (deep, research-grade). Next: ensure slot1 worker drains its X event queue + draws; investigate Expose-on-map / backing-store / compositing mapped windows. Repos: blink=lanmower/blink@libblink-portabox (pushable as lanmower), webix=AnEntrypoint/webix, sandcastle->portabox(file-linked)->webix(github). Host loads wasm from /containers/blinkenlib.{js,wasm} (public/). Dev artifact propagation into node_modules .pnpm is ephemeral; durable = portabox re-pull webix. [[live-x-window-progress]] [[x-client-handshake-progress]]