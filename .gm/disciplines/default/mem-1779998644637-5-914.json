BROWSER BRIDGE WORKS (sandcastle, 2026-05-28, supersedes the earlier 'bridge down' note which was pruned). The playwriter browser spool is healthy: `browser session new` -> session survives between dispatches; raw-JS eval returns values; page.goto(dataURL/http)+page.evaluate+page.title all work (witnessed: goto a data: html, read getElementById textContent='hello-spool', title='t'). NOTE: a bare eval (no page.goto) runs in the Node executor context where navigator/self are absent ('no-nav'); to touch the DOM you MUST page.goto first. The earlier UV_HANDLE_CLOSING crash cleared (stale chromium process). This UNBLOCKS the e2e DOM witnesses (val-sandcastle-e2e, val-browser-witness-display): run `next dev` then page.goto localhost + page.evaluate over the running app, and the framebuffer/input display witness. crossOriginIsolated is false on data: URLs (expected; only true under the COOP/COEP-served app).