X-APPS DISPLAY — CROSS-VM FRAMEBUFFER FRONTIER CLOSED (2026-05-31). The all-zero-Xvfb-fb frontier open across ALL prior sessions is FIXED + pixel-proven. ROOT CAUSE (finally isolated, not "external"): the X server runs on its own worker-VM pthread; its framebuffer host pointer was resolved via SpyAddress(m,fb_vaddr) using the CALLER thread's _Thread_local machine `m`. The host display blit runs on the MAIN thread where `m` is a different/null VM, so the page-walk returned 0 and fbView read all-zero — even though the server rendered correctly (red bytes WERE in the shared heap, browser-608 offset 153026560). NOT the Xvfb patch (root paint always worked), NOT depth/RGB565 (correct), NOT host page-walk. FIX (user authorized lanmower/blink edit): blink commit 31b3b88 on libblink-portabox adds non-thread-local `fb_machine` global captured at syscall 0x5fb (SysBlinkenlibFbRegister, runs on the fb-owning VM thread); get_fb_ptr + new blinkenlib_fb_spy_address resolve against fb_machine. webix commit 33c4918 blink-core.js fbView uses _blinkenlib_fb_spy_address (fallback to spy_address); build-blink.yml exports it; webix 2a84495 rebuilt blinkenlib.wasm a5b2b9eb6a29. PROVEN browser-655: xsetroot -solid red -> ENTIRE 800x600 fb RED (center+corner [255,0,0], fbNonzero=80000), cross-VM fbSpyResolve=152745024 vs old spy=0 (browser-652). ALSO fixed: :99 stale-socket collision on 2nd launch (sandcastle x-launch.ts unlinks /tmp/.X11-unix/X99 + lock before runConcurrent; browser-629/630/631 server stays alive, no "server already running"). REMAINING (precisely diagnosed, not external): forever-DRAWING clients (xclock/xeyes/xcalc/xterm) still show black because (1) blink dual-worker scheduler freezes server gen when a forever-blocking client holds slot1 (browser-679 clean: gen frozen at 12), and (2) client WINDOW content needs compositing to the screen pixmap. One-shot ROOT painters (xsetroot) work fully. Local node_modules artifact propagation is ephemeral; durable path = portabox re-pull webix. Deps: sandcastle->portabox(github:AnEntrypoint/portabox, locally file-linked)->webix(github:AnEntrypoint/webix). Host loads wasm from /containers/blinkenlib.{js,wasm} (public/). [[x-client-handshake-progress]] [[live-x-window-progress]]