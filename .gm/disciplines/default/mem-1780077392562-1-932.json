X-CLIENT architecture chosen (2026-05-29): DUAL-WORKER via blink pthreads. emscripten -pthread shares one wasm instance+memory across threads, so run the X server on one pthread + the X client on another -- each runs its VMs runLoop with NORMAL blocking syscalls (own thread blocks independently, no cooperative-scheduler starvation), sharing MEMFS+host-fds+g_socks so client connects to servers listener + socketpair data flows; preemptive thread scheduling interleaves them. blinkenlib has the threaded build + OnSpawn/SysSpawn pthread machinery. Plan: blinkenlib_run_thread(vm_handle) pthread_creates a runner per machine; blink-core runWorkers(server,client); REVERT the non-blocking poll/select/epoll emscripten hacks (blocking is correct with real threads). The single-thread cooperative scheduler (runConcurrent) starved the server (epoll hits=0, never reached dispatch loop). X SERVER serving + client CONNECT are witnessed.