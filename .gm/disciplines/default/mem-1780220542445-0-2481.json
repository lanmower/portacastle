APK IN-PAGE INSTALL CAPABILITY -- WORKING + WIRED (2026-05-31, session6 browser :3000). User goal: install apks so we can run whatever we want. RESULT: fully working in-page. CHAIN (all live-witnessed): sandcastle x-launch.ts launchXApp -> Sandbox.pkgInstall (portabox sandbox.ts:941 -> webix-client.ts:598 -> host.ts:245 ensureApk/createApk) -> webix/src/alpine-apk.js addByName -> apk-repo.js makeRepo (APKINDEX over CORS proxy chain DEFAULT_PROXIES codetabs/allorigins/corsproxy/wranger, DEFAULT_REPOS alpine v3.21 main+community) -> apk-format.js gunzip/parseTar/writeRecord into guest MEMFS + /lib/apk/db/installed. The GUEST makes no network calls; host orchestrates fetch+extract. import(webix/alpine-apk) resolves (webix github dep transitive via portabox). WITNESSES: codetabs proxy APKINDEX 200/487KB/2.4s; pkgInstall(figlet) 3.66s 66 files; xterm full dep-closure installed (30 pkgs: libx11/libxcb/libxft/fontconfig/freetype/libxaw/...); base rootfs present (/lib/ld-musl-x86_64.so.1, /bin/busybox, /etc/apk); pkgSearch(gui) 584 GUI pkgs; pkgInfo(xterm) v396-r0. METHOD NAMES on the Sandbox handle: pkgSearch/pkgInfo/pkgInstall/pkgRemove/pkgInstalled (NOT search). FIXES SHIPPED (sandcastle): (1) x-launch.ts -- pkgInstall now runs OUTSIDE runExclusive (it is host-orchestrated, no VM lock needed); the old code wrapped it in runExclusive so a not-installed launch queued ~90s behind a forever X client (xeyes) and looked hung -- witnessed install-outside-lock figlet 6.7s WHILE xeyes held the lock; (2) install failure returns {exitCode:'apk-install-failed', stderr:msg} + post-install presence re-check instead of swallowing catch{} and running a missing binary; (3) per-workspace install-progress overlay: x-launch publishes __sc.xinstallByWorkspace[wsId] (Installing <pkg> (apk)... / failure), XWindowCanvas polls it 500ms and shows it with priority -- no more silent connecting/black during install (witnessed overlay text 'Installing xterm (apk)...'); (4) re-added xterm to the dock BUILTIN_APPS (desktop-store.ts) -- reverses the 3aa1892 drop now that install works. PERSISTENCE: installed files live in the host-singleton MEMFS for the page lifetime; a reload resets MEMFS so install repeats per session. tsc+eslint+build:static all 0. NO-EXTERNAL reconcile: apk fetch is via public Alpine mirrors+CORS proxies (network) per explicit user request; the offline bundled x-client-overlay.tar.gz remains the default zero-network path, apk is the opt-in expand layer.