X SERVER WORKS UNDER BLINK IN-PAGE (2026-05-29, WITNESSED CI 26640791802 PX-smoke EXIT TIMEOUT(serving)). The real X server reaches its dispatch loop + serves 30s clean under blink/wasm. WINNING STACK: (1) custom GL-less Xvfb built from xorg-server 21.1.16 source in CI (webix .github/workflows/xorg-patched-build.yml -> containers/Xvfb-patched, 2MB musl ELF) with --disable-glx/dri/glamor (no mesa/LLVM, no OOM) AND a patch to xkb/ddxLoad.c RunXkbComp that SKIPS the xkbcomp fork+Popen and just returns the keymap name so LoadXKM reads a PRECOMPILED server.xkm (generated by host xkbcomp, containers/server.xkm, placed at /tmp/server-99.xkm); (2) blink in-process AF_UNIX socket layer (unixsock_shim) + epoll_shim + finite RLIMIT_NOFILE + -nolock; (3) CRITICAL blink-core.js fix: trampoline the cooperative-preempt resume off the signal-callback stack (setTimeout(0) instead of synchronous _blinkenlib_preempt_resume) -- the forever-running X server was overflowing the JS call stack after ~22s of MAX_CYCLES preempt recursion; now each slice runs on a fresh stack. blink fork stays ENOSYS (not needed). NEXT: launch an X CLIENT (xeyes/xsetroot/xlogo, no keyboard) as a separate top-level runElf with DISPLAY=:99 connecting over the in-process AF_UNIX socket; witness it renders to the fb. Then wire Xvfb+client into sandcastle GuiDesktopApp (worker long-run) + browser-witness. webix build-blink.yml has the PX-smoke step.